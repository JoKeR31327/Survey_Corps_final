const STATES = require("./order.state");

module.exports = {
  [STATES.CREATED]: [STATES.PAYMENT_PENDING, STATES.CANCELLED],
  [STATES.PAYMENT_PENDING]: [STATES.CONFIRMED, STATES.CANCELLED],
  [STATES.CONFIRMED]: [STATES.SHIPPED],
};
